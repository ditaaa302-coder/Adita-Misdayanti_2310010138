name: aplikasipos
description: A new Flutter project.

# The following defines the version and build number for your application.
# ...

environment:
  sdk: '>=3.0.0 <4.0.0' # Sesuaikan dengan SDK Anda

dependencies:
  flutter:
    sdk: flutter

  # Tambahkan DUA PACKAGE INI
  http: ^1.2.1 # Versi saat ini atau yang terbaru
  intl: ^0.19.0 # Versi saat ini atau yang terbaru
  #============================
  
  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

# The rest of the file...